## 02:33 — hello-world build

Processed spec `specs/ready/hello-world.md`. Implemented `projects/hello-world/hello.py` — stdlib-only Python CLI greeting script. All three verification criteria passed. Empty-string name requires explicit `parser.error()` since argparse does not natively reject empty strings. Task moved to `tasks/review/hello-world.md`.

## 10:21 — spec-lifecycle build

Processed spec `specs/ready/spec-lifecycle.md`. This is a meta-spec defining the spec archive protocol — no code artifact, the implementation is the convention itself. Archived spec to `specs/archive/spec-lifecycle.md` before beginning work (applying the very protocol defined by the spec). Updated `memory/builder/MEMORY.md` with the protocol. Task moved to `tasks/review/spec-lifecycle.md` with `spec_archive` field.

## 10:33 — pipeline-next-step build

Processed spec `specs/ready/pipeline-next-step.md`. Added `PIPELINE_DOWNSTREAM` dict and `print_pipeline_next` function to `runtime/factory_runtime/cli.py`. The dict maps each agent to downstream directories with optional actionable commands — single data structure, no scattered conditionals. Function called in `run` command after run-log line, inside the try block (runs on success and NO_REPLY, skipped on exception). Rich formatting: yellow counts, bold commands, dim idle message. Task moved to `tasks/review/pipeline-next-step.md`. Note: `rm` without `-f` ran interactively in background — used `rm -f` to force-delete the ready spec.

## 11:00 — self-review-loop build

Processed spec `specs/ready/self-review-loop.md`. This is an infrastructure spec — no code artifact, only filesystem scaffolding for a new reviewer agent. Created: `memory/reviewer/MEMORY.md`, `memory/daily/reviewer/`, `skills/reviewer/review-protocol/SKILL.md`. Archived spec to `specs/archive/self-review-loop.md`. Task moved to `tasks/review/self-review-loop.md`.

BLOCKER: spec requires adding a `reviewer` entry to `agents.yaml`, but global CLAUDE.md designates `agents.yaml` as READ-ONLY. Did not modify it. The review task documents the exact stanza the human needs to add manually. All other deliverables complete. Human decides how to handle the constraint conflict.

## 11:43 — Three specs built (spec-pipeline-cleanup, failure-learning-convention, seed-skill-gaps)

**spec-pipeline-cleanup**: Added `run_spec_pipeline_cleanup(workspace, dry_run)` and `cleanup-specs` CLI command to `runtime/factory_runtime/cli.py`. Function enforces single-stage invariant — removes upstream spec copies when a downstream stage already holds the same filename. Archive never touched. Integrated as post-execution hook in `run` (every agent). Dry-run validation showed correct behavior: stale files in inbox/drafting identified, unmatched inbox files untouched.

**failure-learning-convention**: Added `extract_failure_learnings(workspace)` to `runtime/factory_runtime/cli.py`. Scans tasks/failed/ for `## Generalizable Learning` sections, writes extractions to learnings/failures/ with deduplication. Logs non-blocking warnings for missing sections. Integrated as post-verifier hook (agent == "verifier"). Proposed `skills/proposed/verifier-verification-protocol/SKILL.md` with mandatory Generalizable Learning requirements and re-verification protocol.

**seed-skill-gaps**: Created 14 SKILL.md files in skills/proposed/ covering all missing agent-specific seed skills. Content derived from factory-blueprint.md descriptions. Each 15-40 lines, valid frontmatter, named `{agent}-{skill-name}` for Librarian routing.

NOTE: `rm` remains aliased to interactive on this system. Used `python3 -c "os.remove()"` to delete files from specs/ready/. Established pattern: use Python for filesystem deletions to avoid interactive alias.

## 12:20 — failed-task-rebuild-convention build

Processed spec `specs/ready/failed-task-rebuild-convention.md`. Added `factory rebuild {task-name}` CLI command to `runtime/factory_runtime/cli.py`.

New: `_extract_section(text, heading)` helper (MULTILINE/DOTALL regex, returns None if absent). `rebuild_task(workspace, task_name) -> (int, str)` pure logic — validates prerequisites (exit 1 if missing, exit 2 if spec already in ready), versions failure report to `.v{N}.md`, copies archived spec to ready, extracts Path to Resolution (fallback: Summary, fallback: full text), extracts N/10 score, writes rebuild brief. `rebuild` Click command wraps this, prints structured output, commits state transition to git.

Updated `run_spec_pipeline_cleanup`: stale rebuild brief check added at end — `{name}.rebuild.md` in ready/ is stale when `{name}.md` absent from ready/ AND present in archive.

Added `shutil` and `subprocess` imports at module level.

Smoke-tested all paths: happy path (correct brief content, versioned report, exit 0), exit-1 (missing prerequisites), exit-2 (spec already in ready), stale brief cleanup.

NOTED COSMETIC ISSUE: `cleanup-specs --dry-run` prefix substitution ("Cleaned stale spec:" → "[dry-run] Would clean:") does not apply to stale rebuild brief lines — they display "Cleaned stale rebuild brief:" in both dry and live modes. Deletion is correctly suppressed in dry-run; this is display-only. Reported in review task for Verifier judgment on scope.

## 13:53 — task-review-cleanup and failed-task-lifecycle build

**task-review-cleanup**: Added `run_task_review_cleanup(workspace, dry_run)` and `factory cleanup-tasks [--dry-run]` to `runtime/factory_runtime/cli.py`. Removes stale `tasks/review/` files when exact-name match found in `tasks/verified/` or active (non-versioned) `tasks/failed/` slot. Versioned `.v{N}.md` files excluded via regex. Hooked into `run` post-execution (every agent). Ran live cleanup — removed 4 stale review files. Idempotent re-run confirmed 0 deletions. Versioned-only non-match edge case confirmed.

**failed-task-lifecycle**: Added `tasks/resolved/` directory (gitkeep, factory init updated). Added `resolve_completed_failures(workspace)` — verifier-only post-execution hook that moves versioned and active failure reports to tasks/resolved/ when the task appears in tasks/verified/, appending `## Resolution / resolved_by: rebuild`. Added `resolve_task(workspace, task_name, reason)` pure logic. Added `factory resolve {task-name} --reason "..."` CLI command with git commit. Updated `factory status` to show `tasks/resolved/`. Post-execution hook ordering now: spec cleanup → task cleanup → failure resolution (verifier) → failure learning extraction (verifier). Applied resolution during build: seed-skill-gaps.v1.md → tasks/resolved/ (rebuild), self-review-loop.md → tasks/resolved/ (manual). tasks/failed/ now empty.

## 12:38 — seed-skill-gaps rebuild

Picked up rebuild from `specs/ready/seed-skill-gaps.rebuild.md`. First build failed at 5/10: 11 of 14 skills exceeded the 40-line content ceiling.

Rebuild approach: editorial compression only — no content rewriting. All 11 skills trimmed to ≤40 lines. Key decisions:
- `spec-spec-patterns` (was 110 lines): dropped 3 duplicate skeleton templates (Web API, Data Pipeline, Library), replaced with 3-line note that the same structure applies; kept CLI skeleton as representative.
- Skills 5-8 lines over: removed worked examples, collapsed multi-line bullets to prose, removed redundant trailing statements.
- `builder-tool-use`: removed "When Tools Fail" section entirely (principle covered by "check exit codes").
- `verifier-failure-reporting`: removed "Tone" section (5 lines, low value relative to ceiling pressure).

Final counts: all 11 between 31-40 lines. 3 passing skills untouched (source-evaluation 37, dependency-management 40, monitoring 40). Removed `specs/ready/seed-skill-gaps.md` and `specs/ready/seed-skill-gaps.rebuild.md`. Updated `tasks/review/seed-skill-gaps.md` with rebuild timestamp.

## 14:21 — human-action-report build

Processed spec `specs/ready/human-action-report.md`. Implemented `factory needs` CLI command for surfacing human-action-needed entries aggregated from all agent `memory/{agent}/needs.md` files.

Key implementation notes:
- `parse_needs_entries()`: splits needs.md files on `^## ` headings; each block parsed for id/status/created/category/blocked/context fields
- `resolve_need()`: regex-locates open entry by ID, mutates status in-place, writes file; returns (exit_code, message)
- `factory needs` command: grouped by category, sorted oldest-first; Rich markup escaping required for literal `[agent, age]` and `[resolved]` brackets (both consumed silently by Rich without `\[`)
- `factory needs --resolve ID` commits with `"Resolve factory need: {id}"` message exactly as spec requires
- Auto-commit during smoke test also picked up initial `memory/builder/needs.md` creation

Created `memory/builder/needs.md` with existing reviewer agents.yaml blocker (from self-review-loop build).
Created `skills/proposed/human-action-needed/SKILL.md` (56 lines, under 100-line ceiling).
Updated `skills/reviewer/review-protocol/SKILL.md`: added Step 1.5 for needs escalation check.

All verification criteria passed. Task at `tasks/review/human-action-report.md`.
