Run the factory librarian agent as a subagent. Use the Task tool with subagent_type "Bash" to execute:

```
cd /Users/david/Projects/fac/factory && .venv/bin/factory run librarian
```

Report the outcome and pipeline-next guidance back to the user. Do not take further action — the user decides what runs next.